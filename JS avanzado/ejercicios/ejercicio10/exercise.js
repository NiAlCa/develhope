let numberStore = [0, 1, 2];
let newNumber = 3;
numberStore = [...numberStore, newNumber];
console.log(numberStore);


/* 
Do you know how to "merge" the value of the `newNumber ` variable into `numberStore` without using the array method push?


¿Sabes cómo "combinar" el valor de la variable `newNumber` en `numberStore` sin usar el método push de matriz?

*/